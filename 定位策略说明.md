# 树节点展开图标定位策略

## 问题分析

### HTML 结构
```html
<div class="radio-1">
    <input data-text="技术与大数据平台部" ...>
    <label for="...">技术与大数据平台部</label>  ← 这是文字
    <span class="jtree_expand jtree_node_open" ...></span>  ← 这是要点击的展开图标
</div>
```

### 关键关系
- `<label>` 和 `<span>` 是**兄弟元素**（在同一个 div 下）
- `<span>` 在 `<label>` 的**后面**
- 因此使用 XPath 的 `following-sibling` 轴

---

## 解决方案（推荐）

### XPath 定位器
```xpath
//label[normalize-space(text())='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]
```

### 分解说明
1. `//label[normalize-space(text())='技术与大数据平台部']`
   - 找到文字为"技术与大数据平台部"的 label 元素
   - `normalize-space()` 去除首尾空格，匹配更准确

2. `/following-sibling::span`
   - 从该 label 开始，查找**后面的兄弟** span 元素
   - `following-sibling` 只查找同级的后面元素

3. `[contains(@class, 'jtree_expand')]`
   - 限定 span 必须包含 `jtree_expand` class
   - 确保点击的是展开图标，不是其他 span

---

## Robot Framework 使用

### 基本用法
```robot
# 方法1: 使用封装好的关键字（推荐）
Click Tree Expand Icon By Text    技术与大数据平台部

# 方法2: 直接使用 XPath
${locator}=    Set Variable    xpath=//label[normalize-space(text())='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]
Click Element    ${locator}
```

### 完整示例
```robot
*** Test Cases ***
选择成本中心
    # 1. 打开成本中心选择树
    Click Element    xpath=//button[contains(., '选择成本中心')]
    Sleep    1s

    # 2. 通过文字定位，点击展开图标
    Click Tree Expand Icon By Text    技术与大数据平台部

    # 3. 继续展开下级节点（如果需要）
    Click Tree Expand Icon By Text    某个子部门

    # 4. 最后选择目标节点
    Click Element    xpath=//label[text()='最终目标部门']
```

---

## 备选方案

### 方案 A：通过 input 的 data-text 属性
```xpath
//input[@data-text='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]
```
- 优点：data-text 属性值干净，无空格问题
- 缺点：依赖 data-text 属性存在

### 方案 B：通过父容器定位
```xpath
//div[contains(@class, 'radio')]//label[text()='技术与大数据平台部']/../span[contains(@class, 'jtree_expand')]
```
- 使用 `..` 返回父元素，再找 span
- 可读性稍差

### 方案 C：模糊文本匹配
```xpath
//label[contains(text(), '技术与大数据平台部')]/following-sibling::span[contains(@class, 'jtree_expand')]
```
- 使用 `contains()` 而不是精确匹配
- 适用于文本前后可能有空格或其他字符的情况

---

## 调试技巧

### 1. 在浏览器 Console 中测试 XPath
```javascript
// 测试定位器是否正确
$x("//label[normalize-space(text())='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]")
// 应该返回 1 个元素

// 查看返回的元素
$x("//label[normalize-space(text())='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]")[0]
```

### 2. 在 Robot Framework 中调试
```robot
# 检查找到多少个元素
${count}=    Get Element Count    xpath=//label[normalize-space(text())='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]
Log    找到 ${count} 个元素    console=True

# 如果找到多个，查看详细信息
Debug - Find Expand Icon Locator    技术与大数据平台部
```

### 3. 验证元素状态
```robot
${class}=    Get Element Attribute    ${locator}    class
Log    节点状态: ${class}    console=True
# jtree_node_close = 未展开
# jtree_node_open = 已展开
```

---

## 常见问题

### Q1: XPath 找不到元素（返回 0 个）
**可能原因：**
- 文字不完全匹配（有隐藏空格、换行符）
- 元素还未加载完成
- label 和 span 不是兄弟关系（DOM 结构不同）

**解决办法：**
```robot
# 使用模糊匹配
//label[contains(text(), '技术与大数据平台部')]/following-sibling::span

# 或者先等待元素
Wait Until Element Is Visible    xpath=//label[text()='技术与大数据平台部']    timeout=10s
```

### Q2: XPath 找到多个元素（例如 23 个）
**原因：**
- 使用了错误的定位策略（如 `//li[contains(., '文字')]//span`）
- `contains(.)` 会匹配所有子孙节点的文本

**解决办法：**
- 使用 `following-sibling` 精确定位兄弟元素
- 使用 `text()` 或 `normalize-space(text())` 而不是 `.`

### Q3: 点击后没反应
**可能原因：**
- 节点已经是展开状态（jtree_node_open）
- 需要等待动画完成

**解决办法：**
```robot
# 检查状态再决定是否点击
${class}=    Get Element Attribute    ${locator}    class
${is_closed}=    Run Keyword And Return Status    Should Contain    ${class}    jtree_node_close
Run Keyword If    ${is_closed}    Click Element    ${locator}
```

---

## 总结

✅ **推荐使用：**
```xpath
//label[normalize-space(text())='技术与大数据平台部']/following-sibling::span[contains(@class, 'jtree_expand')]
```

✅ **优点：**
- 通过文字定位（满足需求）
- 点击右边的展开符号（满足需求）
- 精确匹配，避免找到多个元素
- 健壮性好，处理空格问题

✅ **在 Robot Framework 中使用封装好的关键字：**
```robot
Click Tree Expand Icon By Text    技术与大数据平台部
```

---

## 文件说明

- **TreeSelectorByText.robot** - 包含所有关键字和测试用例
- **定位策略说明.md** - 本文档，详细说明定位策略
